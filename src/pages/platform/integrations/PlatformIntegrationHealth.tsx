/**
 * Platform Integration Health page — Wraps the IntegrationHealthDashboard.
 */
import IntegrationHealthDashboard from '@/modules/traccar/ui/IntegrationHealthDashboard';

export default function PlatformIntegrationHealth() {
  return <IntegrationHealthDashboard />;
}
