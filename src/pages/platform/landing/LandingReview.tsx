/**
 * /platform/landing/review — Director review queue.
 */
import { LandingReviewQueue } from '@/components/platform/landing/LandingReviewQueue';

export default function LandingReview() {
  return <LandingReviewQueue />;
}
